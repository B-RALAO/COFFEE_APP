<h1>All Coffees</h1>

<% if @coffees.nil? == false %>
  <% @coffees.each do |coffee| %>
    <%= coffee.name %>
    <%= coffee.origin %>
    <%= coffee.strength %>
    <%= coffee.rating %>
    <%= coffee.description %>
  <% end %>
<% else %>
  <p>No Coffees yet</p>
<% end %>

<%= link_to "Add new coffee", new_coffee_path %>
<%= link_to "Back to home", root_path %>
<%= link_to "Log Out", destroy_user_session_path, data: {turbo_method: :delete} %>

