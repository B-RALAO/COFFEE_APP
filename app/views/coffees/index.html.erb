<h1>All Coffees</h1>

<% if @coffees.nil? == false %>
  <% @coffees.each do |coffee| %>
    <%= coffee.name %>
    <%= coffee.origin %>
    <%= coffee.strength %>
    <%= coffee.rating %>
    <%= coffee.description %>
  <% end %>
<% else %>
  <p>No Coffees yet</p>
<% end %>

<%= link_to "Add new coffee", new_coffee_path %>
